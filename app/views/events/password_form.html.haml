%h2 Введите код доступа
= form_tag('', method: 'post') do |f|
  .form-group
    = label :pincode, 'Код доступа'
    = text_field_tag :pincode, params[:pincode], class: 'form-control'
  .actions
    = submit_tag 'ОК', class: 'btn btn-primary'
.buffer-top
  = link_to 'На главную', events_path
